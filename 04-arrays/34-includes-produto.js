// EXERCÍCIO 34 - INCLUDES

let produtos = ["Notebook", "Mouse", "Monitor", "Teclado"];

// Verifique se "Monitor" está no array usando includes().
// Mostre "Produto disponível" ou "Produto não disponível".

// Escreva sua solução abaixo:

if (produtos.inclubes("Monitor")){
    console.log("produto disponivel");
} else {
    console.log("produto não disponivel")
}

